import { describe, it } from 'node:test';
import assert from 'node:assert/strict';

describe('health check', () => {
  it('should return ok status', async () => {
    // placeholder — mesa agents should flesh this out
    assert.ok(true, 'placeholder test');
  });
});
